Added stimuli display script, "stimuli.py".  Reads a newline delimited stimuli file (.txt) from current directory, creates a user-specified number of randomized repetitions, and writes a csv output file with [repetition #, stimulus, time].

